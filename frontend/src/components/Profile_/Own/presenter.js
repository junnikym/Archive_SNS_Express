import React from "react";
import PropTypes from "prop-types";

const Own = (props, context) => (

    <div className = "Own_Profile">
    
        {props.info?.email}
    </div>

);

export default Own;