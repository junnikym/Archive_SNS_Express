import React from 'react';

import { BrowserRouter as Router, Route} from 'react-router-dom';

const Profile_ = (props, contet) => (
    

        <Router>   
          test
        </Router>


  )

export default Profile_;