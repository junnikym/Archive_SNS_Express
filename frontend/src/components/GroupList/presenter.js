import React from 'react';
import PropTypes from "prop-types";

const GroupList = (props, content) => (

        <div className = "groupList">
        
            Group List :

            <input
                type = "button"
                name = "groupInvite"
                value = "Invite"
            />
        
    </div>
    
);

export default GroupList;